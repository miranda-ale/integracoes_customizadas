# Integrações (Stub)

A estrutura de integrações (stub, DataJud, etc.) do módulo de **processos judiciais / contencioso** foi movida para o app **Jurídico** (módulo **Processo**).

Para documentação e implementação, consulte o app `juridico` em `apps/juridico/` (ex.: `juridico/processo/integracoes/`).
